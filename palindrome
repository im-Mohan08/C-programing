#include <iostream>
#include<stdio.h>
 using namespace std;
int main()
{
    char ch[101];
    int count=0;
    int flag=0;
    scanf("%s",ch);
     for(int i=0;ch[i]!='\0';i++)
    {
       count++;
    }
    for(int i=0;i<count/2;i++)
    {
        if(ch[i]!=ch[count-1-i])
        {
           printf("not");
           return 0;
        }
    }
    printf("p");
}
